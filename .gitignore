# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
.Python

# Django
*.log
db.sqlite3
db.sqlite3-journal
staticfiles/
media/

# Environment files (CRITICAL - contains DB passwords + API keys)
.env
.env.*
!.env.example

# Database dumps
*.dump
*.sql.gz
litrix_backup*

# Node
node_modules/
.angular/
dist/
.npm

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Litrix specific
*.csv.bak
scraper_logs/
*.log

# Runtime artifacts produced by the scraper / diagnostics scripts.
# These are local working files — never source-of-truth, never PII-safe.
backend/.authorships_cache.json
backend/ghost_papers_review.xlsx
backend/*.xlsx

# Large reference data that lives outside git (downloaded on demand).
# scimagojr2025.csv is 11 MB and updated yearly by the Scimago import.
scimagojr2025.csv
scimagojr*.csv

# Build artifacts
*.egg-info/
build/
*.egg

# Google OAuth client secret + token helpers (NEVER commit)
client_secret*.json
*.apps.googleusercontent.com.json
token.json

# Local data / audit / migration working sets — research data + ops
# artifacts, NOT app source. Kept out of git so deploy.ps1's `git add .`
# never balloons the repo or publishes researcher/paper data.
data/
Scopus_Migration_2026-06/
*.commit_msg_tmp.txt
