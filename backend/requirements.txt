# Litrix Django REST Backend — pinned dependencies
# Versions chosen for stability + compatibility with Python 3.11+

Django==5.0.6
djangorestframework==3.15.1
django-cors-headers==4.3.1
django-filter==24.2
psycopg2-binary==2.9.9
python-dotenv==1.0.1
openpyxl==3.1.5

# --- Production hosting ---
# WSGI server — Render/Railway/Heroku run this instead of `manage.py runserver`
gunicorn==22.0.0
# Serves static files (admin CSS/JS) without needing nginx
whitenoise==6.7.0
# Parses DATABASE_URL env var into Django DATABASES dict
dj-database-url==2.2.0
