Django==5.0.6
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
django-filter==24.2
psycopg2-binary==2.9.9
python-dotenv==1.0.1
openpyxl==3.1.5
httpx==0.27.0
# SerpAPI client — needed by citations/refresh_hybrid.py (admin "Refresh
# Citations" button) when a SerpAPI budget > 0 is requested. Imported
# lazily, so OpenAlex-only runs work without it.
google-search-results==2.4.2

# backend/affiliation_verifier.py (admin "Scrape New Papers" sync pipeline
# runs it after each incremental scrape). `requests` is a hard dependency
# of that script; pypdf + beautifulsoup4 + lxml power its PDF and
# publisher-HTML crawling tiers (imported lazily — without them those tiers
# degrade gracefully to OpenAlex/Crossref only).
requests==2.32.3
pypdf==4.3.1
beautifulsoup4==4.12.3
lxml==5.2.2

gunicorn==22.0.0
whitenoise==6.7.0
dj-database-url==2.2.0
