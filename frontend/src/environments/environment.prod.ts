/**
 * Production environment.
 * Used when building with `ng build` (or `ng build --configuration=production`).
 *
 * IMPORTANT: Replace this URL with your actual deployed Django backend
 * before building for production. e.g. https://litrix-api.onrender.com/api
 */
export const environment = {
  production: true,
  apiBaseUrl: 'https://litrix-api.onrender.com/api',
};
