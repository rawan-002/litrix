/**
 * PostCSS config — wires Tailwind + Autoprefixer into Angular's
 * CSS pipeline. Place in: frontend/postcss.config.js
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
